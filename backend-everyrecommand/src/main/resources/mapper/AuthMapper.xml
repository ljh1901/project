<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ourcommunity.mapper.AuthMapper">
    <select id="findUserByLoginId" resultType="map">
        SELECT user_id AS "userId", login_id AS "loginId",
               password_hash AS "passwordHash", display_name AS "displayName",
               role, is_active AS "isActive"
        FROM app_user
        WHERE login_id = #{loginId}
    </select>
</mapper>
